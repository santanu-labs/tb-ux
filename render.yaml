services:
  - type: web
    name: leave-studio-site
    runtime: node
    rootDir: website
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: REVIEW_USER
        value: review
      - key: REVIEW_PASSWORD
        sync: false
